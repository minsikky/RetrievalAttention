# Current Status

## Scope

- Repository: RetrievalAttention / RetroInfer long-context attention experiments.
- Current phase: make the frontier algorithm benchmark-ready, not open-ended selector search.
- Target evaluation style: dense prefill plus decode-only approximation.
- Target model family for benchmark work: Qwen3-8B / Llama-3.1-8B scale models that fit on A40-class GPUs.

## Active Goal Statement

End state: one benchmark-ready canonical GPU implementation of the current CPU frontier decode algorithm. It keeps dense prefill, approximates decode only after sealed PQ pages are active, matches CPU frontier semantics, and is fast enough to run real dense-vs-frontier task benchmarks.

Canonical algorithm semantics to preserve:

- Prefill attention is dense. Prefill may build/update PQ sidecars, but it must not use sparse/frontier attention.
- Decode uses the CPU frontier algorithm: paged K-PQ fullscan selector, affine-calibrated approximate logits, candidate ranking, exact-K refinement, mixed exact-K/K-PQ logits and probabilities, adaptive K/V output-stability confidence, global residual-risk exact-V selection, and V-PQ reconstruction for non-exact V rows.
- Exact V selection is global residual risk: `risk_i = p_i^2 * V_PQ_error_stat_i`, where `p_i` comes from the mixed exact-K/K-PQ probability distribution.
- Accepted K budgets, accepted V budgets, selected-token counts, logical MB/query, attention outputs, and o-proj outputs must match the CPU reference within parity tolerance.
- Any change to selector scoring, calibration, ranking, budget ladder, confidence/stopping rule, probability construction, exact-V rule, V-PQ reconstruction, or dense-prefill/decode-only scope is a separate algorithm variant, not a canonical optimization.

Implementation approach:

- The benchmark decode hot path must be fused/native CUDA. The target is not a PyTorch-heavy or hybrid PyTorch+CUDA implementation.
- Native CUDA should own decode-critical work: K-PQ scoring, candidate ranking/top-k, exact-K logits, mixed probability construction, adaptive K/V confidence, residual-risk ranking/prefixes, V-PQ aggregation, and final output construction.
- PyTorch is allowed for model orchestration, tensor allocation/lifetime, QKV/O-projection plumbing, extension launch glue, lightweight metadata setup, assertions, reporting, and offline parity/debug checks.
- PyTorch must not dominate the decode loop through repeated hot-path `topk`, `sort`, `gather`, `scatter`, `softmax`, matmul, masking, prefix-sum, or per-budget tensor materialization.
- Dense-logit simulator paths, PyTorch-heavy paths, and partially native paths are useful diagnostics, but they are not the final benchmark path unless profiling proves the remaining PyTorch work is outside the decode-critical path and semantics still match the CPU reference.

Performance goals:

- Overall runtime target: canonical frontier decode should run within `2-3x` dense decode on representative active-path benchmarks while preserving semantics.
- Primary short-decode gate: RULER `niah_single_1`, 32k context, 1 sample, 128 decode tokens, all layers/all heads, Slurm `spgpu`. Dense reference job `50645768` decoded in `17.99s`; required frontier target is `<=54s` (`<=3x`), stretch target `<=36s` (`<=2x`).
- Sustained-decode gate: forced LongGen-style workload with sealed pages active for at least `8192` generated tokens. Dense reference job `50718580` generated 8192 tokens in `323.65s`; required frontier target is `<=971s` (`<=3x`), stretch target `<=647s` (`<=2x`).
- Secondary sustained-scale gate: forced `16384` generated tokens. Dense reference is `702.13s`; required frontier target is `<=2106s` (`<=3x`), stretch target `<=1404s` (`<=2x`).
- Report prefill latency, decode/generation latency, profiling overhead, cost-stat overhead, sidecar/update overhead, logical frontier MB, and physical GPU MB separately.
- After runtime gates pass, run dense-vs-frontier task-quality comparisons on at least one reasoning benchmark, one coding benchmark, and one long-generation benchmark.

Success criteria:

- CUDA unit tests pass for every native helper used by the canonical path.
- Saved-trace CPU-vs-GPU parity passes on real Q/K/V traces at decode lengths `32000`, `64000`, and `128000` on multiple heads.
- Parity covers accepted K/V budgets, selected-token counts, logical MB, attention outputs, and o-proj outputs.
- HF/RULER with `FRONTIER_CANONICAL_GPU=1` exercises the approximation path, not dense fallback, inactive sidecars, or diagnostic shortcuts.
- A paired primary gate exists: one no-stats timing run for latency and one accounting run for logical/physical MB.
- The primary 32k/128 gate reaches `<=54s` decode without semantic changes.
- The sustained 8192-token gate reaches `<=971s` generation time without semantic changes.
- Cost reports separate logical frontier MB from physical GPU execution MB and include selector MB, exact-K/V MB, V-PQ/tail MB, update/sidecar MB, and total step MB/query.
- Profiling shows the decode hot path is native-CUDA dominated and identifies any remaining non-native bottlenecks.
- Benchmark-quality runs report dense and frontier accuracy/score, decode latency, active approximation coverage, logical MB, and physical MB for matched model/task settings.

Constraints / invalid shortcuts:

- Do not replace adaptive confidence with fixed budgets, fixed top-k, selected-mass V, selector-rank exactness, hand-calibrated schedules, context-length-specific knobs, or benchmark-specific knobs and call it canonical.
- Do not use oracle attention probabilities, dense top-k rankings, true achieved mass, relL2 against dense output, task labels, future tokens, generated answers, or post-hoc dense outputs inside selector, compression, confidence, or budget logic.
- Do not compute dense attention output and then mask/prune only for reporting. Dense compute is acceptable only as a clearly labeled diagnostic/simulator baseline, not as the canonical benchmark hot path.
- Do not count physical dense reads as logical sparse reads, and do not hide dense K/V reads, sidecar rebuilds, PQ refreshes, exact probes, PyTorch sort/top-k/gather traffic, online-update work, or calibration/sidecar costs.
- Do not promote an optimization that changes accepted budgets, selected-token statistics, logical MB, or outputs beyond parity tolerance; record it as a separate variant.
- Do not claim benchmark readiness from short smokes, single-layer/head traces, disabled cost stats without paired accounting, inactive approximation paths, runs where sealed PQ pages never activate, or runs that only pass because the context is too short.
- Do not run heavy GPU jobs or extension builds on login nodes; use Slurm `spgpu` with account `zhengya98`.

## Active Algorithm

Canonical frontier path as of 2026-05-22:

- Dense prefill.
- Decode-only paged-PQ selector sidecars.
- Decode query scores all tokens with K-PQ, ranks tokens, and refines selected tokens with exact K.
- Adaptive K and V budgets selected by output-stability confidence, not fixed mass.
- Mixed probabilities are formed from exact selected-K logits plus K-PQ tail logits.
- Exact V rows are selected globally by residual-risk, not selected-set mass:
  `risk_i = p_i^2 * V_PQ_error_stat_i`.
- Non-exact V rows use V-PQ reconstruction.
- Logical frontier MB accounting separated from physical GPU simulator MB.

Reference trace result: `k_first_alternating`, threshold `0.001`, layer 16/all heads, decode lengths `500..128000`, mean logical cost `4.779 MB/head-query`, mean o-proj relL2 `0.001118`, max o-proj relL2 `0.002082`.

Important distinction: the public long-decode task-accuracy batch `public_task_accuracy_qwenstyle_20260521_v1` and the 32k RULER optimization runs from job series `5058xxxx` use the older benchmark wrapper defaults (`geometric_probe_tail_switch`, selected-mass exact V, threshold/proxy gates). Treat them as old-frontier baselines, not as current residual-risk adaptive K/V frontier evidence.

`FRONTIER_CANONICAL_GPU=1` now requires the benchmark wrapper to use `online_confidence_rule=joint_kv_stability` and `selected_value_exact_rule=global_residual_risk`. Noncanonical fixed-budget, selector-rank, or selected-mass fast paths are diagnostic/baseline only.

The benchmark-facing canonical GPU wrapper defaults also enable the validated native residual-risk V-prefix helper plus prewarmed persistent V-PQ sidecars:
`SELECTOR_PQ_JOINT_NATIVE_V_PREFIX=1`,
`SELECTOR_PQ_JOINT_NATIVE_RISK_PREFIX=1`,
`SELECTOR_PQ_JOINT_NATIVE_SCORE_GRID=1`,
`SELECTOR_PQ_JOINT_NATIVE_POLICY=1`,
`SELECTOR_PQ_JOINT_PREWARM_VPQ_SIDECARS=1`,
`SELECTOR_PQ_JOINT_PERSISTENT_VPQ_CACHE=1`.
These preserve the logical frontier algorithm but move score-grid construction, residual-risk V-prefix construction, budget-policy evaluation, and V-PQ sidecar work out of the PyTorch-heavy decode path.

## Latest Useful Results

- CPU trace reference result remains the target: `k_first_alternating`, threshold `0.001`, layer 16/all heads, decode lengths `500..128000`, mean logical cost `4.779 MB/head-query`, mean o-proj relL2 `0.001118`, max o-proj relL2 `0.002082`.
- KV-cache compression proxy comparison completed on the same layer-16 trace. Current frontier remains far lower error at comparable MB: frontier `4.779 MB/head-query`, mean o-proj relL2 `0.001118`; best close-quality compression proxy was KIVI-like b4 with 2048 exact window at `5.224 MB/head-query`, mean o-proj relL2 `0.013028`. Artifacts: `attention_efficiency_result/plots/kv_compression_only_vs_frontier_20260522/`.
- Frontier Pareto sweep completed using deployable confidence/budget knobs. Relaxed frontier reaches `1.374 MB/head-query` at mean o-proj relL2 `0.085406`, but PQ-like s8b6 remains lower MB at `0.557 MB/head-query` with mean o-proj relL2 `0.158352`. This shows the current algorithm has a fullscan sidecar floor around `1.37 MB/head-query`; relaxing confidence alone cannot beat compressed fullscan PQ in the high-error regime. Artifacts: `attention_efficiency_result/plots/frontier_pareto_20260522/`.
- Fine-grained PQ-like sweep completed. Higher-resolution PQ-like points close the gap: `pq_like_s16b8_w128` gives `1.318 MB/head-query`, mean o-proj relL2 `0.087796`, max `0.143266`; relaxed frontier `ultra_tau0.512` gives `1.374 MB/head-query`, mean `0.085406`, max `0.211079`, and `ultra_tau0.064` gives `1.392 MB/head-query`, mean `0.074518`, max `0.165586`. Artifacts: `attention_efficiency_result/plots/frontier_pareto_with_fine_pq_20260522/`.
- Targeted `3-5 MB/head-query` PQ-like sweep completed. In this band the frontier dominates PQ-like fullscan compression proxies on relL2: `pq_like_s32b8_w2048` gives `3.205 MB`, mean o-proj relL2 `0.040817`, while `frontier_current_tau0.004` gives `3.394 MB`, mean `0.003390`; `pq_like_s64b6_w2048` gives `4.120 MB`, mean `0.040509`, while `frontier_current_tau0.002` gives `4.120 MB`, mean `0.001822`. Artifacts: `attention_efficiency_result/plots/frontier_pareto_with_pq_3to5mb_20260522/`.
- Broader Awesome-KV-cache/SOTA family coverage now includes trace proxies for retention/pruning, KVPress scorers, merge/prune, scalar/PQ/VQ, TurboQuant, PM-KVQ, Kitty, TaDA, KVTC/FreqKV-style transform coding, LOOKAT-style sparse channels, GEAR, Lexico, and low-rank compression. Combined plot/table: `attention_efficiency_result/plots/frontier_pareto_sota_existing_algorithms_0to10mb_20260523/`. Under `5 MB/head-query`, the strongest external compression-style points are Kitty `4.828 MB`, mean o-proj relL2 `0.009785`, and TaDA `4.928 MB`, mean `0.013670`; both remain much worse than the current frontier `4.779 MB`, mean `0.001118`. Pruning/merge methods sit around `0.07-0.08` mean relL2 near `4 MB`. Coverage caveats are documented in `notes/kv_cache_compression_coverage.md`.
- Distribution/logit distortion metrics were added to the KV-compression trace runner and joint K/V frontier runner. Representative artifacts: `attention_efficiency_result/plots/distortion_metrics_representative_20260523/`. Current frontier threshold `0.001` now has mean probability JS `0.00012095`, mean KL `0.00067055`, and top-512 mass recall `0.999999`, which helps catch probability-routing failures that output relL2 alone can hide. Non-token-comparable merge/prune rows are excluded from probability/logit plots.
- Local validation after wiring and optimization: Python syntax checks passed for the HF runner and frontier wrappers; `benchmark/audit_benchmark_wrappers.py` passes with canonical defaults.
- Local CPU-mode smoke of `run_joint_kv_cpu_gpu_parity_eval.py` passed on the small 16k trace (`max_attention_relative_L2=0.0`, no failures). This validates the parity harness logic only.
- CUDA selector parity smoke job `50645563` passed on decode 500/head 0 (`max_attention_relative_L2=0.0`, `max_oproj_relative_L2=0.0`, no failures).
- Broader CUDA selector parity job `50645581` passed on decode `500,1000` and heads `0,8` (`max_attention_relative_L2=4.44e-09`, `max_oproj_relative_L2=5.62e-09`, no failures).
- Added an opt-in Torch/GPU policy parity mode to the trace runner so parity now exercises the benchmark-style mixed-logit, residual-risk exact-V, V-PQ output grid instead of only swapping selector backends under the CPU NumPy policy. Smoke job `50665664` passed on decode `500`/head `0` with exact K/V budgets and logical MB matching CPU, max Torch/GPU attention relL2 `1.33e-07`, max o-proj relL2 `1.53e-07`. Broader job `50665954` passed on decodes `500,1000` and heads `0,8` with exact budget/MB matches, max Torch/GPU attention relL2 `4.46e-07`, max o-proj relL2 `2.00e-07`.
- Slurm smoke manifest: `notes/slurm_manifests/jointkv_canonical_smoke_20260522.tsv`.
- First HF canonical smoke passed after the torch joint-K/V rewrite: job `50645631`, layer 16, 4k context, 2 decode tokens, `approx_attention_calls_total=2`, logical step `1.229 MB/head-query`.
- Runtime optimizations landed for the HF canonical path: lazy joint K/V confidence simulation, V-risk prefix sums, GPU-side K-budget construction, bounded per-forward V-PQ cache, split top-K selection from unsorted PQ tail scores, fp32 probability/risk tensors, GQA-batched joint evaluation, vectorized residual-risk code statistics, reduced exact-V fallback gathers, and optional prewarmed V-PQ sidecars for benchmark execution.
- Small 4k all-layer no-profile smoke after bounded-cache fix passed: job `50645773`, decode `17.70s`, logical step `1.238 MB/head-query`, selected `3682.5` tokens/head-query. This is a correctness/runtime smoke only; `MAX_NEW_TOKENS=2` is too short for task quality.
- Dense 32k RULER reference passed: job `50645768`, `niah_single_1`, 128 decode tokens, prefill `8.84s`, decode `17.99s`, score `100.0`.
- Current 32k/4 canonical frontier profile is within the decode-runtime target, but the representative 32k/128 promoted-default run is not. Pre-fp32 job `50645811`, 4 decode tokens, score `100.0`, decode `282.33s`, logical step `4.394 MB/head-query`; fp32-probability job `50645835` improved decode to `225.50s`; GQA-batched fast-index job `50645917` improved to `100.44s`; grid-artifact job `50649188` improved to `72.63s`; vectorized residual-risk plus reduced V fallback gather job `50650042` improved to `58.12s`; residual-reuse job `50651432` improved to `56.60s`; prewarmed V-PQ sidecar job `50652948` improved the profiled 4-token decode to `55.30s`; native V-prefix plus prewarmed persistent V-PQ job `50669920` improved the 4-token decode to `51.30s`. However promoted-default 32k/128 jobs `50671405` and `50671500` decoded in `376.33s` and `368.43s`, about `20.9x` and `20.5x` dense. The benchmark-readiness runtime target is therefore not met.
- Layer-only 32k fp32 diagnostic: job `50645834`, layer 16, 1 decode token, decode `3.59s`, logical step `3.192 MB/head-query`; per-layer costs are still too high (`native_geometric=0.995s`, `native_output=0.910s`, `index_sidecar=0.878s`).
- Current best profiled canonical 32k/4 point: job `50669920`, score `100.0`, decode `51.30s`, prefill `29.57s`, logical step `4.391 MB/head-query`, physical step `9.044 MB/head-query`, selected `13424.5` tokens/head-query. This combines native V-prefix with prewarmed persistent V-PQ sidecars; it moves V-PQ sidecar work upfront and keeps decode inside the `2-3x` dense target for this diagnostic.
- Direct-gather affine calibration was tested and rejected: job `50664352`, score `100.0`, decode `70.39s`, logical step `4.395 MB/head-query`, selected `13437.25` tokens/head-query. Because accepted-budget statistics changed relative to the canonical `13446.875` selected tokens/head-query, the patch was reverted instead of promoted.
- Fine-grained joint-K/V profiling landed: job `50664465`, score `100.0`, logical step `4.397 MB/head-query`, physical step `9.045 MB/head-query`, selected `13446.875` tokens/head-query. It preserves canonical accounting and splits the 32k/4 geometric path into score-grid `2.99s`, probability/base-output `0.93s`, residual-risk/top-k/prefix `7.10s`, and policy `0.71s`. The next native boundary should primarily attack residual-risk exact-V top-k/prefix construction, then score-grid construction.
- Segmented V-prefix reduction is opt-in and not promoted: job `50664601`, score `100.0`, decode `55.97s`, residual-risk prefix `1.66s`, but selected tokens changed to `13430.0` and logical step to `4.394 MB/head-query`. Keep `SELECTOR_PQ_JOINT_SEGMENTED_V_PREFIX=0` for canonical runs unless a parity-preserving version is found.
- Indexed calibration-mask shortcut was tested and removed: job `50664906`, score `100.0`, decode `69.50s`, selected `13437.25` tokens/head-query. It likely collapses duplicate indexed-token positions that the full-context mask preserves, so it is not canonical.
- Native residual-risk V-prefix helper is promoted into the benchmark-facing canonical wrapper defaults. The superseded short unit job `50666562` timed out after the fullscan PQ and GPU V-PQ helper checks printed pass messages, so it is not counted as promotion evidence. Replacement targeted chain:
  - targeted CUDA V-PQ/native-prefix unit: `50667497`, output `cuda_unit_result/frontier_cuda_joint_vprefix_20260522_vpqonly`;
  - native-V-prefix trace parity after unit success: `50667503`, output `attention_efficiency_result/joint_kv_cpu_gpu_parity_20260522/native_vprefix_after_50667497`;
  - 32k/4 HF profile diagnostic after parity success: `50667511`, output `ruler_eval_result/frontier_jointkv_profile_20260522/jointkv_ctx32768_n1_t4_native_vprefix_after_50667503`.
  A lower-resource duplicate unit gate was also submitted because the prior run used only about 2.6 GB RSS: `50667861` -> parity `50667862` -> profile `50667903`. The targeted unit `50667861` passed in `5:13` with `test_gpu_vpq_helpers.py`; the redundant larger chain was canceled. Small-context native-V-prefix parity `50667862` passed on decodes `500,1000` and heads `0,8` with no failures, max CPU/native attention relL2 `4.44e-09`, max CPU/native o-proj relL2 `5.62e-09`, max Torch/GPU-policy attention relL2 `1.85e-06`, and max Torch/GPU-policy o-proj relL2 `1.65e-06`. Long-context parity `50669624` also passed on decodes `32000,64000,128000`, heads `0,8`, with no failures, max CPU/native attention relL2 `4.25e-09`, max CPU/native o-proj relL2 `1.70e-08`, max Torch/GPU-policy attention relL2 `2.72e-06`, and max Torch/GPU-policy o-proj relL2 `2.18e-06`. Native V-prefix alone (`50667903`) was not enough: score `100.0`, decode `63.38s`, logical step `4.391 MB/head-query`, physical step `9.044 MB/head-query`, selected-token mean `13424.5`. Native V-prefix plus prewarmed persistent V-PQ sidecars (`50669920`) is the promoted benchmark default: score `100.0`, decode `51.30s`, logical step `4.391 MB/head-query`, physical step `9.044 MB/head-query`, selected-token mean `13424.5`.
- Native risk-prefix CUDA path is promoted into the benchmark-facing canonical defaults. It replaces the PyTorch residual-risk `topk` plus native prefix helper with a CUDA extension function that builds risk scores, segment-sorts them natively, and accumulates the V-PQ residual correction. Targeted CUDA unit job `50694099`, int32 rebuild unit job `50698906`, and trace parity jobs `50695443` / `50701016` passed. It is now enabled by default as `SELECTOR_PQ_JOINT_NATIVE_RISK_PREFIX=1`.
- Native score-grid CUDA path is promoted into the benchmark-facing canonical defaults. It moves mixed exact-K plus affine-calibrated K-PQ score-grid construction out of repeated PyTorch clone/mask/scatter operations. Low-resource CUDA unit job `50701015`, short trace parity job `50701018`, and long-context trace parity job `50702996` passed over decodes `32000,64000,128000` and heads `0,8`. The 32k/16 HF profile job `50702455` completed with score `100.0`, decode `85.14s`, logical step `4.082 MB/head-query`, physical step `8.973 MB/head-query`, and mean selected tokens `12456.6`. This improves the previous promoted-default 32k/16 profile from `100.68s` to `85.14s` mainly by reducing score-grid time from `15.11s` to `1.35s`; residual-risk prefix remains high at `15.05s`.
- Native joint-K/V policy CUDA path is promoted into the benchmark-facing canonical defaults. Targeted CUDA unit job `50703967` passed; short trace parity job `50704188` passed with no failures and max Torch/GPU-policy attention/o-proj relL2 `1.85e-06` / `1.65e-06`; long-context trace parity job `50704354` passed over decodes `32000,64000,128000` and heads `0,8` with no failures and max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`. It is now enabled by default as `SELECTOR_PQ_JOINT_NATIVE_POLICY=1`.
- Grouped residual-risk V-prefix CUDA helper is implemented but not promoted as a default yet. CUDA unit job `50705567` passed for the new grouped helper. Microbenchmark job `50705919`, shaped like one 32k decode layer with 8 KV groups, 4 K budgets, 4 heads/group, and 7 V budgets, matched exactly (`max_abs_diff=0.0`) and reduced repeated per-group risk-prefix time from `33.96ms` to `8.38ms` (`4.05x`). HF smoke job `50706019` with `SELECTOR_PQ_JOINT_GROUPED_RISK_PREFIX=1` completed on RULER `niah_single_1`, 4k context, 2 decode tokens, and exercised the approximation path (`approx_attention_calls_total=64`); grouped risk-prefix time was `0.099s` on that smoke. Representative 32k/16 profile job `50706105` completed with score `100.0`, decode `62.77s`, risk-prefix `4.53s`, score-grid `1.34s`, policy `0.56s`, and geometric `17.12s`; this improves over `50704367` (`71.49s`, risk-prefix `15.05s`) but is still not benchmark-ready. All-head selector top-k reuse jobs `50706486`/`50706487` were negative (`64.02s` no-stats and selector time increased to `3.12s`), so that patch was reverted. Token-layout caching plus avoiding full cached-V fp32 casts is positive: `50706841` no-stats decoded in `52.80s`, and `50706843` accounting decoded in `53.63s` with logical step `4.081 MB/head-query` and selected `12455.2` tokens/head-query. Full-budget sort elimination jobs `50707166`/`50707182` showed a faster no-stats runtime (`48.20s`) but changed tie-sensitive accepted-token statistics (`4.081782 MB/head-query`, selected `12456.6`), so the optimization is guarded behind `SELECTOR_PQ_JOINT_SKIP_FULL_BUDGET_SORT=1` and is not canonical. All-head ranked-prefix batching also changed accepted stats in job `50707496` (`4.081766 MB/head-query`, selected `12456.25`), so it is guarded behind `SELECTOR_PQ_JOINT_ALLHEAD_RANK_PREFIX=1`, default off; follow-up jobs `50707545`/`50707560`/`50707702` were canceled. Exact full-budget score-grid rows are promoted: `50708291` decoded in `48.02s` with exact canonical accounting (`4.081499 MB/head-query`, selected `12455.15625`), because it keeps full ranked-prefix construction for tie semantics but fills any all-nonbase K-budget row with exact dense logits.
- Earlier no-profile validation did not reveal hidden profiling overhead: prewarmed job `50653460` decoded in `57.11s` with prefill `32.77s`; lighter residual-reuse job `50653944` decoded in `60.45s` with prefill `14.11s`. This is superseded by the native V-prefix + prewarmed persistent V-PQ diagnostic `50669920`, which decoded in `51.30s`; rerun a no-profile representative slice with the promoted defaults before broad task-quality claims.
- Longer 32k/16 no-profile slice shows runtime amortizes but remains far from dense: job `50663127`, score `100.0`, decode `109.51s`, `6.84s/token`, logical step `4.082 MB/head-query`, selected `12457.4`; same slice with `DISABLE_COST_STATS=1`, job `50663143`, score `100.0`, decode `87.07s`, `5.44s/token`. Cost-stat accounting is material overhead for task-quality runs, but disabling it removes MB reporting, so use it only alongside separate profiled accounting runs.
- `SELECTOR_PQ_JOINT_GQA_BATCHED=1`, `SELECTOR_PQ_JOINT_VECTOR_POLICY=1`, `SELECTOR_PQ_JOINT_REUSE_MAX_TOPK=1`, `SELECTOR_PQ_JOINT_GRID_ARTIFACTS=1`, score-only `SELECTOR_PQ_JOINT_ALLHEAD_PRECOMPUTE=1`, `SELECTOR_PQ_JOINT_NATIVE_V_PREFIX=1`, `SELECTOR_PQ_JOINT_NATIVE_RISK_PREFIX=1`, `SELECTOR_PQ_JOINT_NATIVE_SCORE_GRID=1`, `SELECTOR_PQ_JOINT_NATIVE_POLICY=1`, `SELECTOR_PQ_JOINT_PREWARM_VPQ_SIDECARS=1`, and `SELECTOR_PQ_JOINT_PERSISTENT_VPQ_CACHE=1` are now explicit benchmark-wrapper defaults. `SELECTOR_PQ_JOINT_ALLHEAD_EXACT_PRECOMPUTE=0` because exact all-head precompute perturbed accepted-budget statistics and did not improve runtime.
- Pre-transition selected-mass/geometric benchmark results are preserved in `notes/selector_eval_latest_results.md` as noncanonical baselines.

## Current Blockers

- The 32k/128 RULER runtime gate now passes with the canonical fast-token-layout path: accounting job `50721909` scored `100.0`, decoded in `43.05s`, and reported logical step `3.832 MB/head-query` / physical step `8.916 MB/head-query`; no-stats job `50722253` decoded in `43.98s`. Dense reference `50645768` decoded in `17.99s`, so the current 32k gate is `2.39-2.45x` dense and inside the `<=54s` target.
- Sustained 8192-token long-decode now passes with canonical guard-on fast-layout. No-stats timing job `50721920` generated `8192` forced tokens in `750.89s` versus dense `323.65s` (`2.32x`), inside the `<=971s` / `<=3x` target. Paired accounting job `50723383` generated `8192` forced tokens in `842.58s` (`2.60x` dense) and reports logical step `1.762 MB/head-query`, physical step `1.872 MB/head-query`, selector `0.030 MB`, exact K/V `1.726 MB`, tail `0.0058 MB`, selected `4281.1` tokens, active fraction `0.359`. The 16384 no-stats job `50723400` generated `16384` forced tokens in `2650.05s`; against the existing dense 16384 reference `702.13s`, this is about `3.77x`, so the 16k sustained target is still not met.
- Current-code validation passed after the latest fast-layout state: CUDA unit job `50727996` passed all extension tests in `82s`; long-context CPU-vs-GPU parity job `50728005` passed on decodes `32000,64000,128000`, heads `0,8`, with no failures and max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`.
- Need stronger dense-vs-frontier task-quality runs for coding and long-generation benchmarks using the promoted joint-K/V canonical path. Existing unforced AIME24/LiveCodeBench slices mostly stopped before sealed pages and therefore do not validate active frontier quality. Active-path forced 8192-token AIME24 and LiveCodeBench dense/frontier slices completed; AIME is a useful one-sample reasoning smoke, while LiveCodeBench is a runtime-only coding smoke because dense and frontier both failed pass@1.

Representative promoted-default runtime checks:
- `50671405`: RULER `niah_single_1`, 32k, 1 sample, 128 decode tokens, normal MB accounting: score `100.0`, decode `376.33s`, logical step `3.821 MB/head-query`, physical step `8.918 MB/head-query`.
- `50671500`: same slice with `DISABLE_COST_STATS=1`: score `100.0`, decode `368.43s`.
- `50674457`: 32k/16 profiled run: score `100.0`, decode `100.68s`; dominant profiled costs are `native_geometric=35.86s`, with `score_grid=15.11s` and `risk_prefix=17.06s`.
- `50676531`: 32k/16 with grid artifacts disabled: score `100.0`, decode `169.31s`; negative, because lazy recomputation moved `native_joint_policy` to `107.46s`.
- `50676533`: 32k/16 with grid artifacts and vector policy disabled: score `100.0`, decode `145.28s`; negative, still worse than default grid.
- `50679445`: 32k/16 with `JOINT_KV_SCHEDULE_PRESET=coarse` (`K=4096,8192,32768`; `V=2048,8192,16384`): score `100.0`, decode `94.83s`, logical step `5.136 MB/head-query`.
- `50679449`: 32k/16 with `JOINT_KV_SCHEDULE_PRESET=coarse2` (`K=4096,32768`; `V=4096,16384`): score `100.0`, decode `87.74s`, logical step `6.806 MB/head-query`. This is the fastest joint-K/V 32k/16 point so far but pays much more logical MB and is still far from the dense-runtime target.
- `50680169`: 32k/16 with `JOINT_KV_SCHEDULE_PRESET=coarse_v8k`: score `100.0`, decode `83.60s`, logical step `6.522 MB/head-query`; faster because risk-prefix drops to `8.39s`, but too much logical MB and still too slow.
- `50680209`: 32k/16 with `JOINT_KV_SCHEDULE_PRESET=mid_v8k`: score `100.0`, decode `105.53s`, logical step `4.496 MB/head-query`; negative.
- `50684318`: 32k/16 diagnostic with `SELECTOR_PQ_JOINT_UNSORTED_V_PREFIX=1`, which uses unsorted per-budget top-k exact-V sets instead of one sorted max-prefix. Canceled after `13:29` with no output artifact because it had already exceeded the default 32k/16 run time; negative.
- `50684812`: 32k/16 diagnostic with `SELECTOR_PQ_JOINT_FAST_AFFINE_SELECTED=1`: score `100.0`, decode `98.88s`, logical step `4.079 MB/head-query`. Negative; essentially matches default runtime and remains too slow.
- `50684985`: canceled stale diagnostic combining `coarse_v8k` with unsorted V-prefix.
- `50685038`: 32k/16 diagnostic combining `coarse_v8k` with fast affine-selected calibration: score `100.0`, decode `84.45s`, logical step `6.524 MB/head-query`. Faster only by paying much higher logical MB; not a canonical improvement.
- `50687944`: 32k/16 on-demand V-prefix diagnostic: score `100.0`, decode `101.72s`, logical step `4.080 MB/head-query`. Negative; avoiding grid materialization pushed work into policy/lazy recomputation.
- `50688219` and `50688781`: canceled stale pending diagnostics before native risk-prefix CUDA edits to avoid contaminated results.
- `50689164` / `50689320`: duplicate 4k/2 smokes for the incremental V-grid diagnostic. `50689164` was canceled as redundant. `50689320` completed cleanly but did not exercise the frontier path because `PAGE_SIZE=5632` exceeded the 4k context (`approx_attention_calls=0`, `selector_active_fraction=0.0`).
- `50691391`: corrected 4k/2 incremental V-grid smoke with `PAGE_SIZE=2048` and data reuse timed out at `20:04` after writing a summary; it exercised the frontier path (`approx_attention_calls=64`, selector active) but decode was `21.83s` for 2 generated tokens, so this diagnostic is not promising.
- `50696385`: canceled pending 32k/16 HF profile for the first native risk-prefix build after changing the native sort value type from int64 to int32.
- `50696452`: 4k/2 HF smoke for opt-in native risk-prefix CUDA path with `PAGE_SIZE=2048` completed cleanly: score `0.0` like the other two-token smokes, decode `11.21s`, logical step `1.238 MB/head-query`, `approx_attention_calls=64`, risk-prefix `0.43s`, score-grid `1.23s`, policy `0.16s`. This validates end-to-end HF wiring but is not a representative runtime result.
- `50698906`: targeted CUDA unit rebuild/test for the int32 native risk-prefix sort path passed in `00:09:24`.
- `50700199`: canceled duplicate trace parity for int32 native risk-prefix after `50701016` passed.
- `50700188`: canceled duplicate risk-only 32k/16 HF profile to prioritize the combined native score-grid plus risk-prefix profile.
- `50700740` / `50700745` / `50700746`: canceled high-memory duplicate native score-grid validation chain after low-resource unit/parity passed.
- `50701015`: low-resource CUDA unit test for native mixed score-grid passed in `00:14:03`.
- `50701016`: low-resource trace parity for int32 native risk-prefix passed in `00:07:40`.
- `50701018`: low-resource trace parity for native score-grid plus risk-prefix passed in `00:03:45`.
- `50701019`: canceled duplicate 64 GB 32k/16 HF profile after `50702455` started.
- `50702455`: 32 GB 32k/16 HF profile for native score-grid plus risk-prefix passed; score `100.0`, decode `85.14s`, logical step `4.082 MB/head-query`.
- `50702543`: canceled duplicate 30-minute 32k/16 HF profile.
- `50702996`: long-context trace parity for native score-grid plus risk-prefix passed over decodes `32000,64000,128000` and heads `0,8`; max CPU/native attention/o-proj relL2 `8.44e-09` / `2.62e-08`, max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`.
- `50703103`: lazy native score-grid plus risk-prefix policy path is negative; score `100.0`, decode `110.75s`, logical step `4.082 MB/head-query`, physical step `8.973 MB/head-query`, selected `12456.6`. It preserves semantics but moves work into `native_joint_policy=54.70s`, so it is slower than the full native score-grid/risk-prefix profile.
- `50703967`: native joint-K/V policy CUDA unit gate passed.
- `50704188`: short native policy trace parity passed on decodes `500,1000` and heads `0,8`; no failures, max Torch/GPU-policy attention/o-proj relL2 `1.85e-06` / `1.65e-06`.
- `50704354`: long native policy trace parity passed on decodes `32000,64000,128000` and heads `0,8`; no failures, max CPU/native attention/o-proj relL2 `4.25e-09` / `1.70e-08`, max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`.
- `50704367`: 32k/16 HF profile with native score-grid, native risk-prefix, native policy, and cost stats disabled completed; decode `71.49s`, prefill `11.42s`, generated tokens `16.0`, policy time `0.41s`, risk-prefix time `15.05s`. MB fields are intentionally zero because `DISABLE_COST_STATS=1`.
- `50704671`: representative 32k/128 no-profile/no-stats canonical native runtime gate completed; decode `270.75s`, prefill `75.86s`, generated tokens `128.0`. This improves over the older no-stats promoted-default run `50671500` (`368.43s`) but is still about `15.0x` the dense decode reference (`17.99s`) and far outside the `36-54s` target. MB fields are intentionally zero because `DISABLE_COST_STATS=1`.
- `50705567`: CUDA unit test for grouped residual-risk V-prefix passed.
- `50705919`: grouped residual-risk V-prefix microbenchmark passed; repeated per-group `33.96ms`, grouped `8.38ms`, `4.05x`, `max_abs_diff=0.0`.
- `50706019`: 4k/2 HF smoke for opt-in grouped risk-prefix passed; `approx_attention_calls_total=64`, decode `7.06s`, risk-prefix `0.099s`. This is correctness-only; score is not meaningful with only 2 generated tokens.
- `50706105`: 32k/16 HF profile for opt-in grouped risk-prefix completed; score `100.0`, decode `62.77s`, prefill `52.54s`, risk-prefix `4.53s`, geometric `17.12s`.
- `50706486` / `50706487`: all-head selector top-k reuse diagnostics completed but are negative; no-stats decode `64.02s`, accounting decode `66.58s`, logical step `4.081 MB/head-query`, selected `12455.2`; reverted because selector top-k time increased.
- `50706769` / `50706770`: canceled before model import because code changed; superseded by `50706841` / `50706843`.
- `50706841` / `50706843`: grouped risk-prefix plus token-layout caching plus cached-V no-full-fp32-cast validation jobs completed; no-stats decode `52.80s`, accounting decode `53.63s`, logical step `4.081 MB/head-query`, selected `12455.2`.
- `50707166` / `50707182`: full-budget sort elimination validation completed; not promoted because accounting changed selected-token mean and logical MB. The diagnostic flag is `SELECTOR_PQ_JOINT_SKIP_FULL_BUDGET_SORT=1`, default off.
- `50707491` / `50707496`: ranked-prefix timing diagnostics completed; rank-prefix is only about `3-4s` on the 32k/16 profile and all-head batching changed accepted stats, so do not promote.
- `50707545` / `50707560` / `50707702`: all-head ranked-prefix/no-profile diagnostics canceled after `50707496` showed accepted-stat drift.
- `50707774`: clean canonical no-profile/no-stats 32k/16 runtime completed; decode `54.64s`, score `100.0`.
- `50707936`: clean canonical no-profile/no-stats 32k/128 runtime gate completed; decode `127.85s`, score `100.0`, improved from previous `270.75s` but still outside target.
- `50708291` / `50708293`: exact-full-budget score-grid validation. `50708291` completed and is promoted: decode `48.02s`, score `100.0`, logical step `4.081499 MB/head-query`, selected `12455.15625`. `50708293` is redundant/no-stats and may still be running.
- `50708383` / `50708387`: lower-memory exact-full-budget duplicates completed; accounting matches canonical, runtime varied (`55.80s` / `60.16s`).
- `50708523` / `50708524`: grouped V-PQ residual cache plus exact-fullbudget validation completed; accounting matches canonical, but runtime worsened to about `58.35s`, so `SELECTOR_PQ_JOINT_GROUPED_VPQ_CACHE` is diagnostic-only and default off.
- Pending validation after the latest native hot-path cleanup:
  - `50708850`, `50709244`, and `50709265` were canceled while still pending so the native CUDA unit validation can run first.
  - `50709435`: targeted CUDA V-PQ/native-helper unit test including the new grouped-flat joint policy helper.
  - `50709650`: compile-only standard-partition Slurm build for the grouped-flat policy helper completed successfully in `4:18` with exit code `0`.
  - Local follow-up cleanup changed persistent V-PQ sidecar cache growth from `torch.cat` to capacity-backed in-place append for decode suffix extension. This should reduce long-decode Python/Torch copy overhead without changing logical V-PQ semantics. Local Python syntax checks, shell syntax checks, and wrapper audit pass; GPU/runtime validation is still blocked on `50709435`.
  - Diagnostic fused grouped-risk policy path added behind `SELECTOR_PQ_JOINT_FUSED_RISK_POLICY=1`. It keeps the same residual-risk ordering and adaptive policy, but avoids writing the intermediate grouped V-budget output grid before policy selection. Compile-only Slurm build `50710431` passed on `standard` in `244s`. Canonical wrappers default it off and `FRONTIER_CANONICAL_GPU=1` rejects it until promotion. It still needs the pending CUDA unit and representative runtime/parity validation before promotion.
  - Direct benchmark scripts `benchmark/run_longbench_v2_hf.sh` and `benchmark/run_public_longdecode_hf.sh` now carry the same canonical joint-KV defaults as the Slurm wrappers, including prewarmed persistent V-PQ sidecars and diagnostic fused-risk default-off. The wrapper audit now covers these direct scripts and passes.
  - Local decode-hot-path cleanup avoids materializing base-plus-ranked selected-token tensors for each K budget when the native score-grid path is active. The native score-grid only needs take counts, base tokens, and ranked prefixes; accounting still uses selected counts. This should reduce PyTorch allocation/gather overhead without changing selector ranking, score-grid semantics, accepted budgets, or logical MB. Local `py_compile`, wrapper audit, and `git diff --check` pass; GPU parity/runtime validation is pending.
  - Native mixed score-grid construction now receives all K-budget rows, including full-budget exact rows, instead of returning partial rows and using Python `index_copy_` plus per-row exact fills. The existing native kernel already overwrites base/ranked selected tokens with exact logits, so a full-budget row becomes exact without Python post-processing. This moves another per-budget grid construction step out of PyTorch. Trace parity job `50711939` passed on decodes `500,1000`, heads `0,8`, with no failures, max CPU/native attention/o-proj relL2 `4.44e-09` / `5.62e-09`, and max Torch/GPU-policy attention/o-proj relL2 `1.85e-06` / `1.65e-06`. HF profile job `50711825` is still pending.
  - Added batched grouped residual-risk native helpers for the canonical grouped V-prefix path. The promoted grouped-risk path can now call native CUDA with `[groups, k, heads, ...]` tensors and derive row groups in-kernel instead of building explicit row-group IDs and flattened `torch.cat` buffers in Python. Unit parity coverage was added against the existing flat grouped-risk reference. Local `py_compile`, wrapper audit, `git diff --check`, and extension symbol import checks pass. Compile-only Slurm job `50713062` passed in `261s` at `cuda_unit_result/frontier_cuda_ext_build_only_20260523_batched_grouped_risk`. Full GPU unit/parity/runtime validation is pending on `spgpu`.
  - Targeted CUDA unit job `50709435` failed in the Python unit test before validating kernels because `policy_ids` was referenced before assignment in `test_gpu_vpq_helpers.py`. This was a test-ordering bug, not evidence of a CUDA helper mismatch. The test was fixed by defining `policy_ids` before the grouped-risk checks. Retry job `50713581` passed in `4:33` with exit `0`, output `cuda_unit_result/frontier_cuda_grouped_flat_policy_20260523_retry1`, so dependent fused-risk diagnostic profile `50713697` is now pending on priority rather than dependency.
  - The dense-prefill sidecar prewarm path no longer forces `torch.cuda.synchronize()` unless `PROFILE_NATIVE_OPS=1`. The sync was only for timing `index_sidecar_seconds`; gating it should reduce no-profile benchmark overhead without changing PQ sidecars, accepted budgets, logical MB, or outputs. Runtime validation will be covered by the pending 32k/128 gates because Slurm jobs execute the current worktree when they start.
  - Added an opt-in diagnostic score-grid helper, `SELECTOR_PQ_JOINT_SCORE_GRID_NO_EXACT_FILL=1`, that skips the native score-grid's initial full exact-score fill when indexed pages plus base tokens cover the whole context. This is guarded out of `FRONTIER_CANONICAL_GPU=1` until parity/runtime validation promotes it. Compile-only Slurm build `50712158` passed on `standard` in `286s` for the current CUDA extension edits. Long-context trace parity job `50712317` is queued to test this helper on decodes `32000,64000,128000`.
  - The no-fill diagnostic now fails closed without adding GPU syncs in the HF or saved-trace parity paths. Both paths check full-context coverage from CPU-side index intervals and base-token metadata before calling `joint_mixed_score_grid_no_exact_fill`, preserving the safety precondition while avoiding a `torch.all(...).item()` synchronization artifact. Lower-memory long parity duplicate `50714326` passed over decodes `32000,64000,128000`, heads `0,8`, with no failures, max CPU/native attention/o-proj relL2 `4.25e-09` / `1.70e-08`, and max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`. Runtime profile `50714329` is pending.
  - Fixed the native score-grid full-budget-row behavior when the ranked prefix is intentionally truncated. Rows with `k_take_count > ranked_prefix_width` now write exact indexed-token scores instead of calibrated PQ scores, so the skipped-full-sort diagnostic can be tested without changing the full-budget row semantics. Unit coverage was added for normal and no-fill score-grid helpers. Compile-only Slurm build `50712396` passed on `standard` in `264s`. Long-context trace parity job `50712458` passed for `SELECTOR_PQ_JOINT_SKIP_FULL_BUDGET_SORT=1` over decodes `32000,64000,128000`, heads `0,8`, with no failures, max CPU/native attention/o-proj relL2 `4.25e-09` / `1.70e-08`, and max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`. Noncanonical 32k/16 profile job `50712539` completed: decode `54.82s`, rank-prefix `1.77s`, logical step `4.0836 MB/head-query`, selected `12463.75`. This is faster than current profile `50711825` but still changes accepted stats slightly, so keep it diagnostic for now.
  - Fixed a decode-index cache metadata bug: when sealed pages were reused across decode steps, the cached `GPUIndex` now refreshes `pending_start` and `indexed_end` for the current context. This preserves online pending-token semantics as the static suffix slides without rebuilding page PQ data. Canonical 32k/16 profile job `50711825` completed: score `100.0`, decode `61.67s`, logical step `4.0833 MB/head-query`, selected `12462.66`. This is slower than older exact-fullbudget profile `50708291` (`48.02s`) and shows slight accepted-stat drift, so treat it as the current post-cachemetadata baseline/regression to beat.
  - The diagnostic grouped V-PQ sidecar cache was repaired to cache grouped `vhat`, residuals, and residual-risk stats together. Fresh profile job `50711661` completed, but the repaired path is still negative: 32k/16 decode `58.07s`, logical step `4.0836 MB/head-query`, selected `12463.75`, worse and slightly different from the canonical exact-fullbudget point `48.02s`, `4.0815 MB`, selected `12455.16`. Keep `SELECTOR_PQ_JOINT_GROUPED_VPQ_CACHE=0`.
  - Benchmark readiness audit now checks for current CPU-frontier semantics instead of older selected-mass/geometric defaults. It flags non-joint confidence, non-global-residual-risk V selection, approximate prefill, missing canonical guard, non-fullscan selectors, and non-affine calibration. Exact logical cost accounting is no longer treated as a readiness warning.
  - Benchmark readiness audit now reports decode seconds per example and physical GPU step MB, and supports `--max-frontier-decode-seconds`. It also flags `cost-stats-disabled`, `missing-step-cost`, `approx-path-inactive`, and `selector-inactive`, so no-stats timing runs or inactive sealed-page smokes cannot be mistaken for complete benchmark-ready evidence without a paired accounting run. The old 32k/128 no-stats artifact `50707936` is now correctly flagged as `cost-stats-disabled, missing-step-cost, decode>54.000s`. A known inactive 4k smoke is flagged as `canonical-guard-off, approx-path-inactive, selector-inactive`.
  - Benchmark summaries now record the native joint-K/V CUDA path flags inside `pagedpq_config`, and readiness audit rejects missing required native flags or active diagnostic flags such as `skip_full_budget_sort`, `score_grid_no_exact_fill`, `grouped_vpq_cache`, or `fused_risk_policy`. Older artifacts that predate this metadata can no longer prove canonical readiness by themselves; rerun them or use paired current-format artifacts. A synthetic current-format copy of `50708291` passes `--strict`, while the same copy with `fused_risk_policy=1` is flagged as diagnostic.
  - Current-canonical 32k/128 runtime gates were submitted after the readiness-audit fix:
    - `50711076`: no-stats timing, output `ruler_eval_result/frontier_jointkv_profile_20260523/pagedpq_batched_niah_single_1_32768_n1_t128_exactfull_current_noprofile_nostats`;
    - `50711080`: paired accounting, output `ruler_eval_result/frontier_jointkv_profile_20260523/pagedpq_batched_niah_single_1_32768_n1_t128_exactfull_current_accounting`.
  - Current-canonical 32k/128 gates `50711076` and `50711080` completed cleanly. Timing run `50711076` scored `100.0`, generated `128` tokens, prefill `28.65s`, decode `114.36s` (`893 ms/token`). This improves over the previous `127.85s` timing gate but is still `6.36x` the dense decode reference and above the `36-54s` target. Accounting run `50711080` scored `100.0`, decode `116.99s`, logical step `3.8355 MB/head-query`, physical step `8.9179 MB/head-query`, mean selected tokens `11728.16`, selector `0.4199 MB`, exact K/V `3.3357 MB`, tail `0.0799 MB`, update `0.0278 MB/head-query`.
  - Fused-risk diagnostic profile `50713697` completed and is negative: score `100.0`, decode `66.74s`, logical step `4.0833 MB/head-query`, selected `12462.66`. It preserves the current post-cachemetadata stats but is slower than the `50711825` baseline (`61.67s`), so keep `SELECTOR_PQ_JOINT_FUSED_RISK_POLICY=0`.
  - No-fill-only profile `50714329` completed: score `100.0`, decode `52.48s`, logical step `4.0836 MB/head-query`, selected `12463.75`. It is faster but changes accepted stats, so it is not canonical by itself.
  - Combined no-fill plus skip-full-sort profile `50714540` completed: score `100.0`, decode `52.84s`, logical step `4.0833 MB/head-query`, selected `12462.66`. This matches the current post-cachemetadata selected/logical stats while improving runtime, so it is the next promotion candidate. Because both flags were individually diagnostic, combined validation is required before promotion.
  - Combined long trace parity `50714942` passed with `SELECTOR_PQ_JOINT_SCORE_GRID_NO_EXACT_FILL=1` and `SELECTOR_PQ_JOINT_SKIP_FULL_BUDGET_SORT=1`: no failures over decodes `32000,64000,128000`, heads `0,8`; max CPU/native attention/o-proj relL2 `4.25e-09` / `1.70e-08`; max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`.
  - Combined 32k/128 accounting gate `50714944` rejected promotion despite the trace parity pass: score `100.0`, decode `125.98s`, logical step `3.8366 MB/head-query`, physical step `8.9183 MB/head-query`, selected `11730.86`. This is slower and slightly higher-cost than the current canonical `50711080` accounting gate (`116.99s`, `3.8355 MB`, selected `11728.16`), so keep `SELECTOR_PQ_JOINT_SCORE_GRID_NO_EXACT_FILL=0` and `SELECTOR_PQ_JOINT_SKIP_FULL_BUDGET_SORT=0` as canonical defaults. The dependent no-stats timing job `50714945` and detail-profile job `50715090` were canceled.
  - Added profiler-only timing buckets for canonical HF runs: joint precompute, token-layout construction, grouped tensor packing, and grouped accounting/output selection. Local `py_compile`, wrapper audit, and `git diff --check` pass. Submitted canonical 32k/16 detail profile `50715246`.
  - Submitted fresh current-worktree canonical 32k/128 gates with diagnostics off: no-stats timing `50715264` and paired accounting `50715265`. Use these to replace stale runtime evidence if they complete cleanly.
  - Fresh current-worktree canonical 32k/128 gates completed with diagnostics off. No-stats timing `50715264`: score `100.0`, generated `128`, prefill `32.37s`, decode `107.84s` (`842.5 ms/token`). Paired accounting `50715265`: score `100.0`, decode `123.08s`, logical step `3.8366 MB/head-query`, physical step `8.9183 MB/head-query`, selected `11730.67`, selector `0.4199 MB`, exact K/V `3.3368 MB`, tail `0.0799 MB`, update `0.0278 MB/head-query`. This is the current representative canonical gate and remains about `6.0x` dense decode, above the `36-54s` target.
  - Canonical 32k/16 detail profile `50715546` completed: score `100.0`, generated `16`, prefill `34.07s`, decode `55.98s`, logical step `4.0833 MB/head-query`, physical step `8.9727 MB/head-query`, selected `12462.66`. Profiled native buckets: rank-prefix `3.94s`, residual-risk prefix `4.53s`, layout `1.66s`, score-grid `0.85s`, probability/base-output `0.75s`, policy `0.23s`, accounting `0.41s`. Profile synchronization inflates `index_sidecar_seconds` (`34.63s`), so use `50715264` for no-stats runtime.
  - Native rank-prefix helper was implemented behind `SELECTOR_PQ_JOINT_NATIVE_RANK_PREFIX=1`, guarded out of canonical mode until promotion. Unit job `50715889`, short parity `50715928`, and long parity `50715943` passed with no failures; long parity covered decodes `32000,64000,128000`, heads `0,8`, max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`. Runtime did not justify promotion: 32k/16 profile `50715944` preserved logical stats but decoded in `56.69s`, and 32k/128 no-stats `50715949` decoded in `118.93s`, slower than current canonical `107.84s`. Keep `SELECTOR_PQ_JOINT_NATIVE_RANK_PREFIX=0`.
  - Residual-risk top-k prefix helper was implemented behind `SELECTOR_PQ_JOINT_RISK_PREFIX_TOPK=1`, guarded out of canonical mode until promotion. Compile-only build `50716260` and unit job `50716266` passed. The 32k/16 profile `50716270` preserved logical stats but was not better: decode `55.12s`, logical step `4.0833 MB/head-query`, selected `12462.66`, and the targeted risk-prefix timer worsened to `5.04s` versus canonical `4.53s`. Dependent 128-token timing/accounting jobs `50716271` and `50716492` were canceled. Keep `SELECTOR_PQ_JOINT_RISK_PREFIX_TOPK=0`.
  - Parallel interval accumulation for the batched grouped residual-risk V-prefix helper is promoted as the current canonical implementation. The helper keeps the same sorted residual-risk order and V-budget semantics, but parallelizes each V-budget interval across tokens and dimensions instead of serially walking up to `16k` residual tokens per output dimension. Compile `50717368` and VPQ unit `50717376` passed. Batched microbench `50717536` improved the canonical helper shape from `8.39ms` grouped to `2.86ms` batched (`2.93x` versus grouped, `11.88x` versus repeated) with max abs diff `2.91e-05` versus the serial reference. Long trace parity `50717386` passed over decodes `32000,64000,128000`, heads `0,8`, with no failures and max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`.
  - Runtime gate now passes. 32k/16 profile `50717396`: score `100.0`, decode `11.36s`, risk-prefix `1.71s`, rank-prefix `1.78s`, layout `1.72s`, score-grid `0.93s`. 32k/128 no-stats timing `50717408`: score `100.0`, prefill `15.04s`, decode `48.25s` (`376.9 ms/token`), which is `2.68x` the dense `17.99s` decode reference and inside the `<=54s` target. Paired accounting `50717414`: score `100.0`, decode `51.31s`, logical step `3.831 MB/head-query`, physical step `8.916 MB/head-query`, selected `11716.6`, selector `0.420 MB`, exact K/V `3.331 MB`, tail `0.080 MB`, update `0.0278 MB/head-query`. Strict readiness audit passes with no canonical warnings.

## Current Task-Quality Validation

- Public task-quality validation is in progress for the passing canonical CUDA path.
- Current-code validation manifest: `notes/slurm_manifests/frontier_current_validation_20260523.tsv`. CUDA unit job `50727996` passed; long native CPU-vs-GPU parity job `50728005` passed over 32k/64k/128k saved-trace positions.
- Manifest `notes/slurm_manifests/public_longdecode_parallel_vprefix_smoke_20260523.tsv` launched dense/frontier AIME24, LiveCodeBench, and LongGenBench SGT-short smokes.
- AIME24 and LiveCodeBench completed but generated only `2048` tokens and had `approx_attention_calls_total=0`, so they prove wrapper execution only, not active frontier quality.
- LongGenBench dense/frontier jobs `50717675` / `50717676` are still running and should exercise sealed pages.
- A follow-up long-cap valid-quality slice was submitted in `notes/slurm_manifests/public_longdecode_longcap_parallel_vprefix_20260523.tsv`: AIME24 and LiveCodeBench with `MAX_NEW_TOKENS=8192`, no forced generation, dense jobs `50717945` / `50717947`, frontier jobs `50717946` / `50717948`.
- Long-cap AIME24 completed with matching dense/frontier accuracy `100.0`, but the model stopped at `4885` generated tokens, below the canonical `page_size=5632`, so frontier approximation remained inactive (`approx_attention_calls_total=0`). Dense generation took `206.11s`; frontier wrapper generation took `274.03s`.
- Long-cap LiveCodeBench completed with matching dense/frontier pass@1 `1.0`, but the model stopped at `2222` generated tokens, so frontier approximation also remained inactive (`approx_attention_calls_total=0`). Dense generation took `81.48s`; frontier wrapper generation took `81.96s`.
- LongGenBench dense/frontier SGT-short completed with forced `16384` generated tokens. Both had completion rate `1.0` and substring accuracy `0.0`, so this is active-path runtime/cost evidence but weak task-quality evidence because dense also failed the string-retrieval metric. Dense generation took `702.13s` (`13:19` Slurm elapsed). Frontier generation took `4059.36s` (`01:09:35` Slurm elapsed), about `5.8x` dense, so long continuous decode currently fails the `2-3x` dense runtime goal even though the 32k/128 RULER gate passes. Frontier cost: `400680` approximate attention calls, active fraction `0.679`, logical step `2.388 MB/head-query`, physical step `2.873 MB/head-query`, selected `6838.95` tokens/head-query, selector `0.085 MB`, exact K/V `2.286 MB`, tail `0.017 MB`, online update `0.0021 MB/attention-call`.
- Forced LongGen SGT-short 8192-token no-stats timing now has a clean dense/frontier pair. Dense job `50718580` generated `8192` tokens in `323.65s`; canonical frontier job `50718512` generated `8192` tokens in `1297.15s`, about `4.0x` dense, with `105768` approximate attention calls and logical step `1.592 MB/head-query`. This still misses the sustained-decode `<=3x` target.
- Forced LongGen SGT-short 6144-token wall profile job `50718677` completed in `460.73s` generation time. Wall-profile buckets over `32040` approximate attention calls: rank-prefix `40.99s`, policy `32.00s`, score-grid `25.19s`, V-PQ sidecar `21.99s`, layout `19.52s`, exact logits `14.57s`, prob/base `12.55s`, risk-prefix `4.79s`, accounting `3.55s`, selector `1.21s`. The bottleneck is distributed; prob/base is worth optimizing but cannot alone close the sustained-decode gap.
- Diagnostic native V-PQ base-output aggregation behind `SELECTOR_PQ_JOINT_NATIVE_VPQ_BASE=1` was implemented and rejected. Compile `50719507` and targeted V-PQ unit `50719508` passed; active 4k/2 smoke `50719781` passed with `64` approximate calls. The 32k/16 profile `50719782` scored `100.0` but decoded in `13.17s`, slower than the current passing 32k/16 reference `50717396` (`11.36s`), with prob/base `3.38s` and slight logical-stat drift (`4.080 MB/head-query`, selected `12452.7`). Keep the flag default-off and rejected by canonical guard.
- Native no-MB grouped policy helper added for non-sensitivity policies. It preserves the same adaptive output-stability policy while avoiding per-call K/V MB tensors in canonical `k_first_alternating`; sensitivity-greedy still uses real MB tensors. Build `50720311` and targeted V-PQ/unit `50720312` passed. The 32k/16 profile `50720313` scored `100.0`, decoded in `10.59s`, logical step `4.081 MB/head-query`, selected `12453.5`, and policy bucket dropped to `0.033s`. The 32k/128 timing repeat `50720410` scored `100.0` and decoded in `53.88s`, barely inside the `<=54s` gate; paired accounting `50720347` preserved logical stats (`3.831 MB/head-query`, physical `8.916 MB/head-query`, selected `11716.7`) but decoded in `59.01s`. Treat this as a semantics-preserving native-boundary cleanup, not enough to close the sustained long-decode gap.
- Fast-token-layout is now part of the canonical guard/defaults. It preserves the same base/ranked token order under the canonical contiguous-page condition, but avoids building large Python token lists in the decode loop. Canonical 32k/128 fast-layout accounting job `50721909` scored `100.0`, decoded in `43.05s`, and reported logical step `3.832 MB/head-query`, physical step `8.916 MB/head-query`, selector `0.420 MB`, exact K/V `3.332 MB`, tail `0.080 MB`, update `0.0278 MB/head-query`; strict readiness audit passes. No-stats job `50722253` decoded in `43.98s`. Duplicate-V-row collapse was tested and rejected; keep `SELECTOR_PQ_JOINT_COLLAPSE_DUP_V_ROWS=0`.
- Sustained long-decode fast-layout validation: canonical guard-on 8192 timing job `50721920` generated `8192` forced tokens in `750.89s` versus dense `323.65s` (`2.32x`), with `105768` approximate attention calls. Paired accounting job `50723383` generated `8192` forced tokens in `842.58s`, with logical step `1.762 MB/head-query`, physical step `1.872 MB/head-query`, selector `0.030 MB`, exact K/V `1.726 MB`, tail `0.0058 MB`, selected `4281.1` tokens, and active fraction `0.359`; strict readiness audit passes for a `971s` sustained threshold. This passes the sustained `<=3x` gate for 8192 tokens. The 16384 no-stats timing job `50723400` generated `16384` forced tokens in `2650.05s`, about `3.77x` the existing dense 16384 reference `702.13s`, so 16k sustained decode still needs more runtime work. The 6144-token wall-profile fast-layout diagnostic decoded in `490.41s` and reduced token-layout wall time from `23.98s` to `4.49s`; remaining wall-profile costs are distributed across score-grid, V-PQ sidecar, exact logits, prob/base, rank-prefix, and accounting.
- Forced active-path AIME24 now has a dense/frontier pair. Dense job `50723500`: accuracy `100.0`, forced `8192` generated tokens, generation `323.80s`. Frontier job `50723501`: accuracy `100.0`, forced `8192` generated tokens, generation `663.66s` (`2.05x` dense), approximation active fraction `0.302`. This is a useful reasoning-quality smoke with active sealed pages, but it is still one forced sample.
- Forced active-path LiveCodeBench completed but is weak quality evidence because both dense and frontier pass@1 are `0.0`. Dense job `50723511`: forced `8192` generated tokens, generation `320.84s`. Frontier job `50723512`: forced `8192` generated tokens, generation `840.88s` (`2.62x` dense), approximation active fraction `0.328`.
- Because offset 0 is not a useful coding-quality comparison, a follow-up forced-8192 LiveCodeBench shard set completed for offsets 1-4, dense and frontier paired: `notes/slurm_manifests/public_task_livecode_offsets1_4_force8192_fastlayout_20260523.tsv` (`50728180`-`50728187`). Offset 2 is the useful coding-quality pair: dense pass@1 `1.0` in `319.22s`; frontier pass@1 `1.0` in `773.82s` (`2.42x` dense), approximation active fraction `0.312`. Offsets 1, 3, and 4 are runtime-only coding smokes because dense and frontier both failed pass@1; frontier generation times were `804.42s`, `801.19s`, and `879.70s`.
- Current 8192-token wall-profile diagnostic `50728570` completed in `751.90s` generation time. Over `105768` approximate attention calls, summed wall buckets across layers were: patched attention `499.48s`, joint total `422.15s`, score-grid `74.68s`, V-PQ sidecar `65.50s`, exact logits `45.76s`, prob/base `41.41s`, rank-prefix `36.83s`, risk-prefix `13.90s`, accounting `11.23s`, layout `11.26s`. The remaining bottleneck is distributed; the next diagnostic targets the PyTorch `softmax + base-output matmul` boundary with an opt-in native helper.
- Native `joint_softmax_base_outputs` is promoted into the canonical wrapper/guard as `SELECTOR_PQ_JOINT_NATIVE_SOFTMAX_BASE=1`. Build `50729310`, VPQ/unit `50729328`, and long saved-trace parity `50729386` passed. Representative RULER 32k/128 gates passed: no-stats job `50730117` decoded in `44.02s`, and paired accounting job `50730118` decoded in `47.12s` with logical step `3.836 MB/head-query`, physical step `8.918 MB/head-query`, and selected `11730.3` tokens/head-query. Sustained LongGen 8192 gates also pass: no-stats job `50730291` generated in `710.06s` (`2.19x` dense), and accounting job `50730303` generated in `815.34s` with logical step `1.762 MB/head-query`. The current wall profile `50730305` shows prob/base reduced to `16.63s`, with the remaining larger buckets in score-grid, V-PQ sidecar, exact logits, and rank-prefix.
- New long-decode V-PQ sidecar grow-pad diagnostic is running as job `50729770` (`SELECTOR_PQ_JOINT_PERSISTENT_VPQ_CACHE_GROW_PAD=8192`). Rationale: the default grow pad is `256`, which can cause repeated persistent sidecar realloc/copy cycles during long decode without changing algorithm semantics. Manifest: `notes/slurm_manifests/vpq_growpad_validation_20260523.tsv`.
- Incremental V-PQ sidecar refresh across sealed-page cache keys was tested and rejected as a canonical optimization. Job `50730919` completed cleanly on forced LongGenBench SGT-short 8192 with `FRONTIER_CANONICAL_GPU=1`, native softmax/base enabled, and wall profiling: generation `791.29s`, joint total `422.76s`, V-PQ sidecar `69.12s`, score-grid `77.33s`, exact logits `52.90s`, prob/base `15.95s`, rank-prefix `39.42s`, risk-prefix `18.28s`. This is effectively unchanged from the native-softmax wall profile (`50730305`: generation `788.07s`, V-PQ sidecar `69.71s`). The cross-key incremental refresh path is now guarded behind `SELECTOR_PQ_JOINT_INCREMENTAL_VPQ_SIDECAR=1`, default off and rejected by `FRONTIER_CANONICAL_GPU=1`; the promoted capacity-backed same-key persistent cache remains.
- Submitted current-canonical native-softmax sustained 16k validation with the incremental V-PQ diagnostic explicitly off. Manifest: `notes/slurm_manifests/current_native_softmax_16k_validation_20260523.tsv`; jobs `50730973` no-stats timing and `50730974` wall profile. Use these to replace the older fast-layout-only 16k timing result and identify the real 16k bottlenecks.
- Submitted one noncanonical grouped V-PQ cache diagnostic because V-PQ sidecar handling remains a large long-decode wall bucket. Manifest: `notes/slurm_manifests/grouped_vpq_cache_diagnostic_20260523.tsv`; job `50730991`, forced LongGenBench SGT-short 8192 wall profile, `FRONTIER_CANONICAL_GPU=0`, `SELECTOR_PQ_JOINT_GROUPED_VPQ_CACHE=1`. This is diagnostic only and must not be promoted without parity/accounting validation.
- Added a diagnostic rank-position native score-grid helper behind `SELECTOR_PQ_JOINT_RANKPOS_SCORE_GRID=1`. It preserves the same mixed exact-K/K-PQ score-grid semantics but replaces the large `[K-budgets, heads, context]` selected-token mask with `[heads, context]` rank positions plus a base mask. Canonical mode rejects it until validation. Local `py_compile`, shell syntax checks, wrapper audit, and `git diff --check` pass. Validation manifest: `notes/slurm_manifests/rankpos_scoregrid_validation_20260523.tsv`; jobs `50731751` VPQ/unit build, `50731755` long saved-trace parity, `50731756` 32k/128 timing, `50731869` 32k/128 accounting, and `50731891` LongGen 8192 wall profile. Runtime jobs depend on parity success.
- Rank-position score-grid validation is not a 32k/128 promotion candidate so far. Unit job `50731751` passed in `4:19`; long saved-trace parity job `50731755` passed over 32k/64k/128k with no failures, max CPU/native attention/o-proj relL2 `4.25e-09` / `1.70e-08`, and max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`. RULER 32k/128 timing job `50731756` decoded in `45.14s`, and accounting job `50731869` decoded in `48.72s` with logical step `3.8361 MB/head-query` and selected `11730.29`; both are slightly slower than the current canonical native-softmax gates (`44.02s` no-stats, `47.12s` accounting). LongGen 8192 wall job `50731891` is still running.
- Rank-position 8192 wall job `50731891` completed in `734.36s` generation time with wall buckets: joint total `402.60s`, score-grid `82.33s`, V-PQ sidecar `65.71s`, exact logits `48.77s`, prob/base `15.05s`, rank-prefix `36.76s`, risk-prefix `13.82s`. This does not improve the score-grid bucket versus the canonical native-softmax wall profile (`50730305`: score-grid `76.48s`), so keep `SELECTOR_PQ_JOINT_RANKPOS_SCORE_GRID=0`.
- Current-canonical native-softmax 16k validation completed and still misses the sustained-scale runtime target. No-stats job `50730973` generated 16384 forced tokens in `2565.56s`, about `3.65x` the dense 16k reference `702.13s`; wall-profile job `50730974` generated in `2686.69s`. The 16k wall buckets are distributed but dominated by score-grid `497.84s`, rank-prefix `322.28s`, V-PQ sidecar `261.92s`, exact logits `198.98s`, and prob/base `60.91s` over `400680` approximate attention calls.
- Grouped V-PQ cache remains rejected. Accounting job `50731290` generated 8192 forced tokens in `854.99s` with logical step `1.7619 MB/head-query`, worse than current canonical accounting `50730303` (`815.34s`, same logical step). Wall job `50730991` lowered the V-PQ sidecar bucket (`48.37s` vs canonical `69.71s`) but end-to-end generation `752.68s` did not beat the canonical no-stats timing (`710.06s`).
- Added a follow-up diagnostic fused rank-position mixed-softmax/base helper behind `SELECTOR_PQ_JOINT_RANKPOS_SCORE_GRID=1` plus `SELECTOR_PQ_JOINT_FUSED_SOFTMAX_BASE=1`. This keeps the rank-position selected-token semantics but skips materializing the full score grid before softmax/base-output construction. Canonical mode still rejects fused softmax until validation. Local `py_compile`, shell syntax checks, and `git diff --check` pass. Validation manifest: `notes/slurm_manifests/rankpos_fused_softmax_validation_20260523.tsv`; unit job `50732614` passed in `4:23`; long saved-trace parity job `50732809` passed with no failures over 32k/64k/128k, max CPU/native attention/o-proj relL2 `4.25e-09` / `1.70e-08`, and max Torch/GPU-policy attention/o-proj relL2 `3.00e-06` / `2.17e-06`; dependent runtime jobs `50732822` 32k/128 timing, `50732842` 32k/128 accounting, and `50732851` LongGen 8192 wall profile are pending.
- Fused rank-position is also negative. No-stats job `50732822` decoded in `46.79s`; accounting job `50732842` decoded in `50.55s` with logical step `3.8361 MB/head-query` and selected `11730.29`. LongGen 8192 wall job `50732851` generated in `750.18s`; it reduced prob/base to `0.62s` but increased score-grid to `93.13s`, leaving joint total `412.49s`. This is slower than canonical native-softmax and non-fused rank-position with no logical-stat advantage. Keep `SELECTOR_PQ_JOINT_FUSED_SOFTMAX_BASE=0`.
- Submitted long-bottleneck diagnostics to target the sustained 16k wall buckets directly. Manifest: `notes/slurm_manifests/long_bottleneck_diagnostics_20260523.tsv`; jobs: `50733608` exact-logit dense-sim 16k no-stats, `50733642` exact-logit dense-sim 8192 wall, `50733647` native-rank-prefix 8192 wall, `50733665` no-fill/skip score-grid 8192 wall, `50733673` combined native-rank-prefix plus no-fill/skip parity, and dependent combo wall `50733675`. Combo parity `50733673` passed with no failures over 32k/64k/128k, max CPU/native attention/o-proj relL2 `4.25e-09` / `1.70e-08`, and max Torch/GPU-policy attention/o-proj relL2 `2.62e-06` / `2.14e-06`.
- Long-bottleneck diagnostic updates: exact-logit dense-sim 8192 wall job `50733642` generated in `784.42s` and did not improve canonical timing; native-rank-prefix 8192 wall job `50733647` generated in `908.41s` and is clearly negative; no-fill plus skip-full-sort 8192 wall job `50733665` generated in `755.43s`, only a small noncanonical wall-profile improvement. Pending combo runtime job `50733675` was canceled after the native-rank component proved negative.
- Submitted no-fill-only validation because the no-exact-fill score-grid helper should be semantically identical under full indexed/base coverage and may reduce the score-grid wall bucket without changing accepted budgets. Manifest: `notes/slurm_manifests/nofill_candidate_validation_20260523.tsv`; jobs `50734367` 32k/128 timing, `50734368` paired 32k/128 accounting, and `50734366` forced LongGenBench 16k timing. Keep `SELECTOR_PQ_JOINT_SCORE_GRID_NO_EXACT_FILL=0` as canonical until these runs prove parity-like logical stats and runtime benefit.
- Submitted a separate fused mixed-softmax/base diagnostic without rank-position metadata, because the rank-position fused path proved the fused softmax/base helper but made score-grid worse. Manifest: `notes/slurm_manifests/fused_softmax_base_validation_20260523.tsv`; jobs `50733865` long parity, `50733868` 32k/128 timing, `50733887` 32k/128 accounting, and `50733889` LongGen 8192 wall.
- Reporting cleanup: no-stats benchmark summaries now include `approx_path_active_fraction` and fall back to approximate-call coverage for selector/tail active fractions when detailed cost counters are disabled. `benchmark/audit_benchmark_readiness.py` also reports the metric name for public benchmark quality, e.g. substring accuracy or pass@1. This only changes summary/reporting; it does not alter selector logic, accepted budgets, outputs, or MB accounting.
- Current passing-gate readiness audit is archived at `notes/archive/benchmark_audits_2026-05/readiness_fastlayout_gates_20260523.md`. It includes the canonical 32k/128 accounting gate and the canonical 8192 forced long-decode accounting gate, both with readiness `ok`.

## Historical Source

The full pre-cleanup append-only status file is preserved at `notes/archive/status_history/current_status_2026-05-20_full.md`.
